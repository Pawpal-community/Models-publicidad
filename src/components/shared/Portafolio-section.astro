---
import Paragraph from "./Paragraph.astro";
import Image from "astro/components/Image.astro";
import { type ImageMetadata } from "astro";

export interface Props {
  title: string;
  images: { src: ImageMetadata; alt: string }[];
}

const { title, images } = Astro.props;
---

<li class="">
  <Paragraph
    className=" text-center text-base mt-10 md:text-lg max-w-3xl mx-auto font-semibold text-gray-800 dark:text-gray-200"
  >
    {title}
  </Paragraph>

  <div class="flex lg:flex-row flex-col gap-4 lg:gap-8  items-center justify-center">
    {images.map((image) => (
      <Image
        src={image.src}
        alt={image.alt}
        class="lg:w-full max-h-[450px] rounded-3xl object-contain mt-4"
      />
    ))}
  </div>
</li>
